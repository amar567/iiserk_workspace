import { Component } from "react";

export default class Util extends Component {
    state = {  }
    render() {
        return (
            <div style={{color:'white'}}>Util</div>
        );
    }
}